import { mkdir, cp, writeFile, rm } from 'node:fs/promises';
import { spawn } from 'node:child_process';
const out='generated/contracts-project'; await rm(out,{recursive:true,force:true}); await mkdir(out,{recursive:true}); await cp('packages/project-templates/angular-template',out,{recursive:true});
const base=`${out}/src/app/pages/contract-list`; await mkdir(base,{recursive:true});
await writeFile(`${base}/contract-list.models.ts`,`export interface Contract { number: string; supplier: string; status: string; value: string; }\n`);
await writeFile(`${base}/contract-list.mock.ts`,`import { Contract } from './contract-list.models';\nexport const contractsMock: readonly Contract[] = [{"number":"CTR-001","supplier":"Acme Ltda","status":"Ativo","value":"R$ 12.000,00"},{"number":"CTR-002","supplier":"Globex","status":"Pendente","value":"R$ 8.500,00"}];\n`);
await writeFile(`${base}/contract-list.page.ts`,`import { ChangeDetectionStrategy, Component, signal } from '@angular/core';\nimport { DsAlertComponent, DsButtonComponent, DsEmptyStateComponent, DsLoadingComponent, DsPageHeaderComponent, DsPaginationComponent, DsSearchInputComponent, DsSelectComponent, DsTableComponent } from '../../design-system';\nimport { contractsMock } from './contract-list.mock';\n@Component({ selector: 'app-contract-list', standalone: true, imports: [DsAlertComponent, DsButtonComponent, DsEmptyStateComponent, DsLoadingComponent, DsPageHeaderComponent, DsPaginationComponent, DsSearchInputComponent, DsSelectComponent, DsTableComponent], templateUrl: './contract-list.page.html', styleUrl: './contract-list.page.scss', changeDetection: ChangeDetectionStrategy.OnPush })\nexport class ContractListPage { readonly loading = signal(false); readonly empty = signal(false); readonly error = signal(false); readonly loaded = signal(true); readonly page = signal(1); readonly contracts = contractsMock; readonly statusOptions = [{ label: 'Todos', value: '' }, { label: 'Ativo', value: 'Ativo' }, { label: 'Pendente', value: 'Pendente' }]; readonly contractColumns = [{ key: 'number', label: 'Número' }, { key: 'supplier', label: 'Fornecedor' }, { key: 'status', label: 'Status' }, { key: 'value', label: 'Valor' }]; }\n`);
await writeFile(`${base}/contract-list.page.html`,`<section class="region region-header"><ds-page-header title="Contratos" description="Gestão de contratos"><ds-button label="Novo contrato" variant="primary" size="md"></ds-button></ds-page-header></section>\n<section class="region region-search"><ds-search-input label="Pesquisa" placeholder="Número ou fornecedor"></ds-search-input></section>\n<section class="region region-filters"><ds-select label="Status" [options]="statusOptions" value=""></ds-select></section>\n<section class="region region-content"><ds-table [columns]="contractColumns" [rows]="contracts"></ds-table></section>\n<section class="region region-pagination"><ds-pagination [page]="page()" [pageSize]="10" [total]="contracts.length"></ds-pagination></section>\n<section class="region region-loading"><ds-loading label="Carregando contratos"></ds-loading></section>\n<section class="region region-empty"><ds-empty-state title="Nenhum contrato encontrado" description="Ajuste os filtros ou cadastre um contrato."></ds-empty-state></section>\n<section class="region region-error"><ds-alert title="Erro ao carregar" message="Não foi possível carregar os contratos." tone="danger"></ds-alert></section>\n`);
await writeFile(`${base}/contract-list.page.scss`,'.region { margin-bottom: 1rem; }\n');
await writeFile(`${base}/contract-list.page.spec.ts`,`import { ContractListPage } from './contract-list.page';\ndescribe('ContractListPage', () => { it('exposes mocked contracts', () => { const page = new ContractListPage(); expect(page.contracts.length).toBeGreaterThan(0); }); });\n`);
await writeFile(`${out}/src/app/app.routes.ts`,`import { Routes } from '@angular/router';\nexport const routes: Routes = [\n  {\n    path: 'contratos',\n    loadComponent: () => import('./pages/contract-list/contract-list.page').then((module) => module.ContractListPage),\n  },\n];\n`);
await mkdir('storage',{recursive:true}); await new Promise((res,rej)=>{const c=spawn('zip',['-qr','../../storage/contracts-project.zip','.'],{cwd:out}); c.on('close',code=>code===0?res():rej(new Error('zip failed')));});
await writeFile('storage/contracts-generation.json',JSON.stringify({status:'COMPLETED',zipPath:'storage/contracts-project.zip',files:['contract-list.page.ts','contract-list.page.html','contract-list.page.scss','contract-list.models.ts','contract-list.mock.ts','contract-list.page.spec.ts']},null,2));
console.log('generated contracts project and zip');
