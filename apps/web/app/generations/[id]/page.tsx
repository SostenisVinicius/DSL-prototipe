export default function GenerationDetail({params}:{params:{id:string}}){return <main><h1>Geração {params.id}</h1><div className="card"><p>Status por polling: consulte <code>/api/generations?id={params.id}</code>.</p><p>Logs, DSL, arquivos gerados, build e ZIP são gravados em <code>storage/contracts-generation.json</code>.</p><a href="/api/generations/download">Baixar ZIP</a></div></main>}
